# Repository Instructions

- Always run `npm run lint` and `npm test` before preparing a pull request message.
- Aim to avoid regressing code coverage with your changes.
- Continue iterating on pull requests until there are no coverage, lint, or test regressions.
- Keep the UI focused on the product experience: avoid adding explanatory text that merely describes design changes or developer intent.
