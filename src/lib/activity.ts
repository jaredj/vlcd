import type { ActivityLevel } from '../types';

export const ACTIVITY_LABELS: Record<ActivityLevel, string> = {
  minimal: 'Recovery / minimal',
  light: 'Light movement',
  moderate: 'Moderate aerobic'
};
